<div class="bar" />

<style>
    .bar {
        height: 5px;
        background: var(--active--bar-color);
        border: var(--active--bar-border-size) solid
            var(--active--bar-border-color);
        z-index: 1;
        width: auto;
    }
</style>
